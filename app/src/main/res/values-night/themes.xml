<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BuildYourOwnMeal" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/main_color</item>
        <item name="android:fontFamily">@font/inter_18pt_extrabold</item>
        <item name="colorAccent">@color/black</item>
        <!-- <item name="android:statusBarColor"></item> -->
    </style>

    <style name="admin" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="android:fontFamily">@font/inter_18pt_extrabold</item>
        <item name="colorAccent">@color/black</item>
        <!-- <item name="android:statusBarColor"></item> -->
    </style>

    <!-- SPLASHSCREEN -->
    <style name="splashscreenBackground" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splashscreen</item>
    </style>

</resources>